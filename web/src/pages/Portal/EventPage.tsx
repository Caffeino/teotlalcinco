import MainLayout from '../../components/layout/portal/MainLayout';

const EventPage = ({ state }: { state: string }) => {
	return <MainLayout state={state}>EventPage</MainLayout>;
};

export default EventPage;
